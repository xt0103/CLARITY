"use client";

import { AppShell } from "@/components/layout/AppShell";

export default function PluginPage() {
  return (
    <AppShell>
      <div style={{ maxWidth: 900, margin: "0 auto" }}>
        <h1 style={{ marginTop: 0 }}>Plugin</h1>
        <div style={{ color: "#64748b" }}>
          Placeholder for future integrations (e.g. browser extension, ATS imports, calendar, etc.).
        </div>
      </div>
    </AppShell>
  );
}

